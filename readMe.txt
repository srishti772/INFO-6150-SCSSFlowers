1. CSS Grid - used in about us section and spring gallery section
2. CSS flexbox - used in foter and after about us section.
3. Variables -In config file, used variables to define colors
4. Custom Properties - In fornt file, used custom properties to define font
5. Nesting - Used in style, buttons
6. Interpolation - Used to generate gallery items and icons
7. Placeholder Selectors - used in font-setting (h1,h2) in font.scss and buttons
8. Mixins - Used to generate position,gallery items and icons
9. Functions - to define button color using dark funtion and shadow for grid
10. Add 3-4 more SASS/SCSS Features of your choice
10.1. Inheritance used in Buttons by extendin one block and adding additional functionalities
10.2. Sass Map - for defining custom font sizes other than inbuilt sizes.
10.3. & operator : for links in menu item for grid item coloring (nth-child)
10.4. Directive @return @if @else for setting font color in @function (font scss)
11. Folder structure hierarchy - SASS modules used to create config, font, scrollbar, main, icons etc.